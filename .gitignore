# Local .terraform directories
**/.terraform/*

# Terraform state files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Terraform variable files (may contain sensitive data)
*.tfvars
*.tfvars.json

# Override files (typically used for local overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan output files (if you use the -out flag with terraform plan)
# Example: *tfplan*

# Terraform CLI configuration files
.terraformrc
terraform.rc

# Terraform dependency lock file (optional, depends on your preference)
# Should .terraform.lock.hcl be committed or ignored? It is a best practice to add this file to .gitignore
# to prevent it from being included in the Git repository.
.terraform.lock.hcl

# Operating system specific files
.DS_Store # macOS

**/logs
*.log

.env

*.pyc
__pycache__/

htmlcov/